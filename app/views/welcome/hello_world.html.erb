<!DOCTYPE html>
<html>
  <head>
    <title>GolfTournamentApp</title>
    <style type="text/css">
      #header-wrap {
        background: #eeeeff;
        position: fixed;
        width: 98%;
        height: 50px;
        top: 0;
        z-index: 3;
        padding-right: 20px
      }
      .splash-container {
        z-index: 2;
        overflow: hidden;
        /* "scroll-over" effect */
        width: 100%;
        height: 88%;
        top: 0;
        left: 0;
        position: fixed !important;
      }
      .splash-container-image {
        background-image: url(<%= asset_path 'harambe_golf.png' %>);
        background-size: 100% 100%;
        width: 100%;
        height:100%;
      }
      .table-container {
        display: table;
        width: 100%;
      }
      .left-half {
        position: absolute;
        left: 0px;
        width: 50%;
        font-weight: 400;
        font-size:250%;
      }
      .right-half {
        position: absolute;
        right: 0px;
        width: 50%;
        position: absolute; bottom: 0; right: 0; text-align:right;
      }
      .content-wrapper {
          position: absolute;
          top: 87%;
          width: 100%;
          min-height: 12%;
          z-index: 2;
          background: white;
      }
      .content {
        padding: 1em;
      }
      .content-head {
        font-weight: 400;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin: 2em 0 1em;
        text-align: center;
      }
      .content-card {
        
        font-weight: 400;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: 20px;
        text-align: center;
      }
      .content-card-title {
        border-style: solid;
        font-weight: 400;
        text-transform: uppercase;
        letter-spacing: 0.1em;
        width:50%;
        margin:0 auto;
        text-align: center;
        align-self: center
      }
      .button {
        padding: 10px 32px;
        text-align: center;
        border-radius: 4px;
        border: 2px solid #4CAF50;
      }
      .button:hover {
        background-color: #4CAF50;
        color: white;
      }
    </style>
  </head>

  <body>
    <div id="header-wrap">
      <div class="table-container">
        <div class="left-half">
          Welcome to Harambe Golf Society
        </div>
        <div class="right-half">
          <button class="button" type="button">Login</button>
          <button class="button" type="button">Signup</button>
        </div>
      </div>
    </div>
    <div class="splash-container">
      <div class="splash-container-image">
      </div>       
    </div>
    <div class="content-wrapper">
      <div class="content">
        <div class="content-head">
          Sort by [Start-Date]
        </div>
        <% @tournaments.each do |tournament| %>
          <tr>
            <div class="content-card-title">
              <%= tournament.start_date.strftime("%m/%d/%Y at %I:%M%p") %>
            </div>
            <div class="content-card">
              <td><%= tournament.title %></td>
              <br/>
              <td><%= tournament.description %></td>
            </div>
          </tr>
        <% end %>
      </div>
    </div>
  </body>
</html>
